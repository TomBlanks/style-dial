{
  "name": "design-tweaker-panel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Floating design-token tweak panel (dev only). Built to a single dependency-free IIFE.",
  "scripts": {
    "build": "node build.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^30.1.1",
    "typescript": "^5.9.3",
    "vitest": "^5.0.1"
  }
}
